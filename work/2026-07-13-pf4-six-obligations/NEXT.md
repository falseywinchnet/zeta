# Next analytical obligations

1. Name the new sixth-jet invariant in the fifth collision coefficient and
   derive an absolute tail bound for it from the order-eight theta jet.
2. Prove a quantitative collision radius by comparing the exact cubic margin,
   the reduced fourth coefficient, and the new fifth invariant.  Cover the
   near-gap complement of the resolved boxes.
3. Attack `D_b>=0` directly.  It is necessary for `J_b>=0` when the right
   endpoint escapes, and a counterexample would decisively retire the stronger
   full Peano density while leaving `J>=0` available.
4. Cover boxes in which only one of `U,V` is at least two.  Keep the separated
   variable in coefficient form and use the collision lemma only for the
   small gap.
5. Treat the compact core with analytic interval subdivisions only after the
   boundary lemmas are complete.  Such subdivisions must certify inequalities;
   point samples remain diagnostics.

