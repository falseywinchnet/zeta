# Next proof obligations

1. Implement a sparse univariate epsilon-series ring for the 74-term `J_b`
   numerator.  Do not fully expand all endpoint remainder variables.
2. Prove exact divisibility by `alpha*beta^2*epsilon^5` and identify the
   divided constant as `(1+2 beta)q^2 C4/12`.
3. Bound only the order-six-and-higher coefficient norm using theta jets
   through order eight.  This gives a uniform simultaneous-collision radius.
4. Insert the resulting lower radial gap into the dominant `J_b` polynomial,
   retaining both angular factors, and run the two-stage rational shift used
   successfully for `S_r`.
5. Treat the two angular faces separately; do not identify them by reflection.
