# Actual-kernel transport closure

- Mode: advancement
- Date: 2026-07-22
- Model: OpenAI Codex
- Starting MIND IDs: R181, R202, R203, R204, R205, CERT19, CERT22,
  CERT23, CERT24
- Starting boundary: `PF4-CORE-v1` is complete, but the maintained
  determinant/transport route ends at a generic theorem whose literal global
  Riemann-kernel instance is not publicly exported.
- Question: can the maintained global kernel jets and universal exact
  `q`, `F2`, and `C4` signs close the transport derivative theorem for every
  ordered triple `x < m < r`, without new hypotheses or range assumptions?
- Status: closed; maintained integration, focused audit, full build, and
  CERT25 replay complete

## No-cheating gates

- no sampled or bounded parameter domain;
- no abstract positivity, differentiability, continuity, or range premise in
  the exported actual-kernel theorem;
- no global surjectivity claim for the curvature coordinate;
- no RH consequence;
- no reopening of T1, T2, or T3.

## Paper consequence boundary

This round may identify a shorter checked route. It does not silently rewrite
the submitted manuscript or its evidence graph. Paper changes belong to the
backport series recorded in `backport/README.md`.

## Result

- `PF4.GlobalStrictPF4.actualKernelSigns` is now the public, premise-free
  actual-kernel `q`, `F2`, and `C4` handoff.
- `globalRiemannKernel_coordinateNumerator_pos` proves the literal coordinate
  numerator is positive for every `x < m < r`.
- `globalRiemannKernel_coordinatePartialXiPsi_neg` proves the literal
  coordinate-`Psi` derivative expression is negative for every `x < m < r`.
- Focused axiom output is exactly `propext`, `Classical.choice`, and
  `Quot.sound` for all three exports.
- The complete Lean build passes 3733 jobs, and CERT25 seals the literal
  actual-kernel transport exports.
- PO-0040 and PO-0041 are closed. PO-0032 through PO-0036 are retained but
  deprecated as optional crossing interpretation because PO-0037 supplies the
  deterministic closed gap directly.
