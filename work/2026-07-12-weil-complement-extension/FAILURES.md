# Boundary

The compact route is retained through its exact positive-operator inequality
and completed `a=0.5` compression. No full `a=0.5` lower bound is recorded.

The relative route is retained through its exact cluster Schur condition and
the observed `a=1` cluster. No finite-cutoff continuation is scheduled.

Numbers printed as zero at the head of the `a=1` spectrum are below double
precision. The recorded content there is cluster size, gap scale, and coupling
scale.
