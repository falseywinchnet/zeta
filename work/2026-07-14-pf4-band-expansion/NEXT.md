# Next proof obligations

1. Tile `0.015<=a,b<=0.095` across the intermediate anchors
   `0.1<|m|<0.48`, using `0.01` gap cells only where `0.02` cells fail.
2. Derive left- and right-face Taylor models in the vanishing gap itself and
   join them at `a=0.015` and `b=0.015` to the separated chart.
3. Add successive large-gap bands above `0.095` until they overlap the bounded
   escape lemmas; use affine gaps and named join boxes.
4. Extend the endpoint-local theta jet toward `|m|=1`; switch to the certified
   x-frame tail jet before direct ratios become inefficient.
5. Merge collision, face, separated, tail, and escape manifests only after all
   overlaps replay with zero unresolved cells.
6. Preserve `global_pf4_claim=false` until the full compact atlas is closed.
