# Results

- Replaced the symmetric global endpoint remainder by componentwise bounds on
  each actual local endpoint interval.
- Kept `g_0=1` and its positive derivatives exact.
- Resolved all four P000054 endpoint failures at `|m|≈0.5`.
- Extended both asymmetric separated ribbons across `-0.5<=m<=0.5`.
- Filled the entire `0.015<=a,b<=0.095` gap rectangle for `|m|<=0.1`.
- Filled the same complete gap rectangle on both endpoint anchor boxes near
  `|m|=0.5`.
- Generated and independently replayed 388 directed boxes with zero unresolved
  boxes and hashed evaluator dependencies.

Global PF4 is not claimed.
