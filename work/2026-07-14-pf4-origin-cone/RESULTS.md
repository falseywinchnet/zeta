# Results

- Proved symbolically that the normalized fully confluent determinant is
  `C4/12`.
- Proved directed uniform bounds for `g` through `g''''` on `[-1,1]`.
- Obtained the rational all-angle collision radius
  `521709/221765037660117511415088107903908000`.
- Proved the x-confluent divided determinant positive for every gap split with
  `0<=r-x<=rho0`, including `theta=0` and `theta=1`.
- Joined this cone to the already-certified same-tail charts using
  `a23<999/1000`.
- Isolated the remaining compact separated domain exactly in
  `(m,rho,theta)`.
- Verified the existing radial and angular boundary identities and certified
  three representative Taylor-model cells, including an origin-crossing cell.

This advancement does not prove the sign on the full compact complement.
