# Next proof obligations

1. Certify the unique crossing of the exact three-mode `C5` polynomial along
   `y=e^-3x`, `z=e^-8x`, and bound the `n>=4` perturbation through derivative
   order nine. This should yield a short rational interval for `u_c` and a
   proof of the complete sign classification.
2. Certify the finite tangency with rational boxes for `D5`, `partial_h D5`,
   `partial_u D5`, and `partial_h^2 D5`.
3. Prove that no second negative component occurs outside the tangency box.
   The natural decomposition is by which of the nine Toeplitz arguments is
   closest to the origin, followed by a one-mode dominant-tail estimate.
4. Only after those audits should either threshold be promoted into the paper
   or MIND as a theorem.

