# Next proof obligations

1. Build a quantitative origin collision cone throughout
   `-R_64<x<m<r<R_64` from the certified global `C4` margin.
2. Preserve and prove the two angular boundary functions of
   `J/[b rho^2]`; reflection does not identify them.
3. Cover the separated compact complement by dependency-aware Taylor charts,
   storing a proof manifest rather than a point scan.
4. Prove exact overlap between the origin cone, separated cells, positive
   tail, global escape, and mirror regions.
5. Audit P000049--P000051 in refine mode and promote the escape/mirror theorem
   only after independent replay.
