# Next

1. Prove the one-crossing theorem for `G_omega` uniformly on compact
   `omega`-intervals inside `(0,1/2)`.
2. Express the positive and negative lobes by explicit beta-integral envelopes.
3. Formulate the weakest bounds on
   `C_omega(t)=sum_{n<=t}c_omega(n)` that imply the pairing inequality.
4. Attempt a direct multiplicative allocation using
   `c_omega(n)=n^omega product_{p|n}(1-p^(-2omega))`.
5. If either method requires a discrepancy estimate equivalent to excluding
   zeros of `zeta(s+omega)`, record that equivalence exactly and move to the
   remaining factorization route rather than hiding the circle.
