# Next theorem

Continue in advancement mode without restoring the monolithic finite-margin
file.

1. Generate and compile `PF4.CERT12F2Margins` from the F2 core/mid and
   F2-margin core/mid blocks only.
2. Generate and compile `PF4.CERT12C4Margins` from the C4 core and C4-margin
   core blocks only.
3. Promote the seven base-coordinate upper/lower Bernstein certificates as a
   separate `PF4.CERT12BaseBounds` module.
4. Define the exact three-mode normalized jet in a maintained module and prove
   its coordinate identity with the base-certificate polynomials.
5. Use `PF4.CERT12ThetaTail` plus `PF4.CERT12PerturbationBounds` to transfer the
   q/F2/C4 margins to the literal infinite theta jet on the compact domain.
6. Promote the unscaled half-strip certificates separately and close the outer
   half-line before exporting global literal signs.

Acceptance remains universal quantification over the analytic domains.  Exact
finite tables are acceptable only through the maintained Bernstein semantics;
no sampled range may occur in the final sign theorem.
