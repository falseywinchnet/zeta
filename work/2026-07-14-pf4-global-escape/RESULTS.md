# Results

- Upgraded the compact Peano estimate to the global comparison
  `7/9 <= D/B <= 1`.
- Proved the global translated-rate bound `|T log H|<=52`.
- Proved `J>0` for every `x<1`, every `x<m<r`, whenever
  `pi exp(2r)>=64`; with R162 this closes right escape for all triples.
- Proved `J>0` whenever `-1<=r<=R_64` and `pi exp(-2x)>=64`, uniformly in
  `m`, where `R_64=log(64/pi)/2`.
- Reduced every central or mixed-sign triple with `r>=-1` to the compact
  ordered chart `-R_64<x<m<r<R_64`.
- Derived the exact reflected numerator.  Reflection maps the certified
  left-end `Psi_+` criterion to a right-end `Psi_-` criterion; it does not
  silently certify the all-negative chart.

No escape-variable scan was used.  Status is advancement proof support,
pending independent refine-round certification.
