# Next theorem

Continue in advancement mode with the compact certificate algebra.

1. Prove exact identities for `clearedQ`, `clearedF2`, and `clearedC4` applied
   to `CERT12Coordinates.twoModeJet`, rewriting them as the maintained margin
   numerators divided by positive powers of `2 * certX t - 3`.
2. Consume `CERT12QMargins` and `CERT12F2Margins` on their complete compact
   bands.  Promote the preserved C4 intermediate-band Bernstein certificates
   as a separate maintained module before using them; do not reconstruct the
   former monolith.
3. Apply `CERT12PerturbationBounds` using all three literal inputs now proved:
   `normalizedSeriesJetVector_eq`, `abs_twoModeJet_le_coreB`, and
   `abs_fullTailJet_le_coreE`.
4. Export strict normalized q/F2/C4 signs for every `t >= 0` with
   `certX t <= 5`, without sign premises, sampled meshes, finite tail cutoffs,
   or floating-point decisions.
5. Then promote the unscaled half-line certificates and close the outer band.
