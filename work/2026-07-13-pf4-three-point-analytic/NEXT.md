# Next analytical steps

1. Expand the division-free `G` in the Peano representations of `lambda` and
   `H`.  Search algebraically for a symmetrized density whose diagonal is
   `q C4` and whose lower-order factors are `q`, `h=F2/q^2`, and `C3`.
2. Derive a one-dimensional Peano formula for the edge quantity
   `L+P'/P-TL/L`.  Establish exactly which new local density appears after
   integration by parts.
3. Express `G` in gap variables with all removable denominators cleared and
   derive its small-gap expansion through the first rigorously positive
   remainder term, using the existing `C4`, `C4'`, and `C4''` machinery.
4. Prove the same-tail escape lemma as a perturbation of
   `q=c exp(plus-or-minus 2t)`, using the existing `E2` through `E6` theta-tail
   bounds.  Work with `G`, not sampled values of `partial_x Psi`.
5. Treat the mixed-tail cases by reflection and endpoint dominance.  Identify
   a finite compact residue only after the analytic escape thresholds are
   explicit.
6. On any compact residue, seek a theta-mode same-sign decomposition modeled
   on Csordas--Varga.  Do not replace this with a dense parameter sweep.

