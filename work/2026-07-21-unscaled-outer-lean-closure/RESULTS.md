# Results

The all-in-one replay proves, without `sorry` or new axioms:

1. the absolute derivative-polynomial envelope for every `j<=6` on `s>=5`;
2. a table-free two-mode outer coordinate bound;
3. an absolute bound on the literal complete infinite tail retaining
   `x^j exp(-8*x)`;
4. exact weighted perturbation budgets for cleared `q`, `F2`, and `C4`, each
   decreasing on the full outer half-line and strictly below its margin;
5. direct uniform outer two-mode margins, including handwritten decreasing
   ratio proofs for `q` and `C4`;
6. strict positivity of all three cleared polynomials of the literal
   normalized infinite theta-series jet for every `t>=0`;
7. transport through the previous parity/global-jet bridge to strict
   positivity of the terminal quotient for the actual global Riemann kernel,
   every real evaluation point, and every ordered `a<c<b<d`.

The endpoint `x=5` is used only after Lean proves the relevant functions are
decreasing on `[5,infinity)`.  No finite search or unproved limit enters.

This closes the CERT12 literal sign boundary PO-0011--PO-0013.  It does not by
itself state T1's determinant quantifiers; final generic-engine assembly and
the separate PF5 obstruction remain outside this round.
