# Next boundary

The positive-side infinite-series jet is now constructed. Do not revisit its
summability or expand the six recursive polynomials.

## Immediate refinement

Audit this advancement artifact and integrate the generic polynomial bound,
bounded-interval control, and `derivativeTowerThroughSix_at_nonneg` into the
maintained Lean project. Preserve the narrow import graph and the one-process
compilation rule.

## Next mathematical advancement

Connect the literal positive-side series to the global Riemann kernel without
defining the latter by `thetaSeries |t|`, whose smoothness at zero would be
circular. The preferred boundary is:

1. formalize the relevant theta modular identity;
2. construct the global even continuation and prove equality with
   `thetaSeries` for `t >= 0`;
3. transport the positive-side derivative jet across zero by the proved
   continuation/parity identities;
4. identify its raw derivatives with the `Phi0,...,Phi6` inputs of the cleared
   certificate bridge.

The three strict cleared signs remain a later independent certificate boundary.
Do not infer them from convergence, parity, or the derivative recurrence.
