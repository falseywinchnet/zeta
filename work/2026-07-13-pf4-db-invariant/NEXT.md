# Next analytical steps

1. Extract an explicit lower monomial from the 1,863-term dominant `J_b`
   polynomial and the 308-term dominant `S_r` polynomial in terms of the gap
   variables `a=U-1`, `c=V-1`.
2. Bound the full-theta perturbation of those cleared numerators away from
   `a=c=0` using the order-eight `rho` bound proved this round.
3. In the remaining collision cone, combine the positive cubic `C4` term,
   the exact fourth coefficient, and the `N6` decomposition with a directed
   sixth-order Taylor remainder.
4. Join the two regions at an explicit analytic threshold.  Repeat under
   reflection with the forced signs of odd jets.
5. Once the full tail is closed, the remaining PF4 task is a compact
   three-variable inequality.  Design interval subdivision around the exact
   numerator `J`, using analytic collision faces rather than sampling them.

