# Next

Resume Lean only after using this theorem decomposition.

1. Prove the simple envelope `|P_j(s)| <= 2^(j+1)s^(j+1)` for `s>=5` by the
   fourteen shifted positive-coefficient cases.
2. Derive the two-mode coordinate bound algebraically; do not import the
   fourteen generated outer Bernstein tables.
3. Prove the complete `n>=3` geometric tail bound with ratio `<1/2`.
4. Add one generic weighted-homogeneous perturbation lemma producing
   `x^w B(exp(-8x))`.
5. Prove its monotonic endpoint reduction for weights `2,6,12`.
6. Add direct decreasing-correction certificates for the cleared `q` and
   `C4` outer margins; reuse the existing exact `F2` half-strip theorem.
7. Combine with P000137's compact literal theorem and then with the maintained
   parity/global-kernel bridge.

The final exported statement must quantify over every `t>=0` and contain no
tail, range, margin, or certificate premise.
