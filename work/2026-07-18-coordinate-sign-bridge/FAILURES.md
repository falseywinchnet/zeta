# Failed and rejected formulations

## Detached mixed derivative

The first tempting theorem would take
`partial_p (T Lambda)=-(Q'(p) delta+T delta)` as a scalar premise. That merely
renames the commutation step in the paper and was rejected. The accepted
candidate defines `T Lambda` by endpoint chord moments and differentiates that
function directly.

## Unqualified logarithmic rate

The equality `N=delta Lambda expandedTransportK` initially omitted
`delta != 0` and `Lambda != 0`. This is false as a field simplification of the
expanded object because `expandedTransportK` contains divisions by both
normalizers. The theorem now exposes both nonzero hypotheses; the coordinate
assembly derives them from strict curvature.

## Cached elaboration and function subtraction

Direct conversion between pointwise subtraction and subtraction in the
function module produced typeclass-level equality goals. The proof now uses an
explicit eventual equality before applying the derivative congruence theorem.
No axiom or placeholder was introduced.

## Remaining edge

The central identity between `expandedTransportK` and the deterministic
transport integral is not asserted as proved. It is the single named premise
of the conditional PO-0041 assembly theorem and is the next advancement
boundary.

