# Next proof obligations

1. Generate compact common-subexpression forms for the 24-term numerator of
   `S_r` and the 74-term numerator of `J_b`, with exact denominator identities.
2. Substitute the normalized positive-tail variables `X,U,V` and isolate the
   pure-exponential positive parts `2q(r)` and
   `q(x)(B+C)(3B+C)`.
3. Prove resolved-gap tail positivity from the explicit `E2`-`E5` error boxes.
4. Bound the fourth- and fifth-order collision remainders uniformly using the
   existing `C4,C4',C4''` machinery and the order-eight theta jet.
5. Use reflection for the negative tail and derive endpoint-dominance formulas
   for mixed-tail configurations.
6. If either monotonicity density changes sign, preserve the counterexample and
   return to direct positivity of `J`; do not infer failure of PF4.

