# Next proof obligations

1. Derive a large-gap formula whose primitive interval variables retain the
   shared exponential/theta dependence; do not widen `J`, its denominators,
   and Gaussian pivots independently.
2. Certify the first band above `0.095` with an adaptive analytic atlas and
   name its closed overlap with the present `a=0.095` or `b=0.095` boundary.
3. Locate the exact compact escape thresholds and tile successive bands only
   until both gap directions meet already proved escape regions.
4. Extend the endpoint-local theta jet beyond `|m|=0.5` and establish a
   replayed overlap with the certified x-frame tail chart.
5. Assemble a coverage ledger for collision, face, bounded separated,
   large-gap, x-frame, and escape manifests; require every boundary face to
   belong to at least one replayed closed box.
6. Preserve `global_pf4_claim=false` until that ledger has no open cell and
   every constituent certificate replays from its manifest.
