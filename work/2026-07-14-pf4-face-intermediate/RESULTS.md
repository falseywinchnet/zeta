# Results

- Closed the missing intermediate-anchor rectangle
  `0.1<=|m|<=0.48`, `0.015<=a,b<=0.095`.
- Added regular left and right Hermite charts through the exact faces `b=0`
  and `a=0`, and verified their joins to the separated chart.
- Added an all-Hermite chart at the simultaneous collision `a=b=0` and a
  conditioned Hermite/separated atlas for the whole corner square.
- Completed `0<=a,b<=0.095` for every `|m|<=0.5` when combined with P000055.
- Generated 6,441 directed interval boxes at 512-bit precision, degree ten,
  with 16 theta terms and zero unresolved boxes.
- Stored exact bounds, chosen charts, environment fingerprint, evaluator
  hashes, expected outcome hash, and replay commands in `manifest.json`.

`global_pf4_claim=false`.  The result proves only the stated compact regional
cover.
