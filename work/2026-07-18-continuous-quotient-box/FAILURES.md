# Failure ledger

No failed Lean proof survived the round. The proof deliberately uses explicit
row determinants and repeated one-variable fundamental-theorem steps instead
of an arbitrary-order Fubini/multilinear abstraction. That abstraction may be
useful later, but it is unnecessary for the fixed order-four boundary and
would enlarge the trusted interface without strengthening the present result.
