# Next

## Ground-state mean

Quantify the small-`a` positivity-improving theorem. The target is an explicit
range containing `a=0.3` and a bound

`|integral w_a|^2/2>=p_*>0`

for the normalized ground state. Combine logarithmic Fourier energy,
anti-concentration, and the spectral separation of the positive ground state.

## Centered positive spectrum

The Hilbert--Schmidt norm sees both signs of `J_a`. Only its positive spectral
part lowers the form. Seek a cosine-transform or signed-kernel decomposition
that bounds `lambda_max(J_a)` directly.

## Piecewise arithmetic flow

Carry the improved smooth estimate into the P000018 active-translation formula.
The next structural milestone is a single explicit comparison law valid up to
and across `a=(log 2)/2`, with the entering prime ramp controlled by endpoint
overlap.

## Independent route

The arithmetic `L2` convolution criterion remains live if ground-state
anti-concentration does not produce a transportable barrier.
