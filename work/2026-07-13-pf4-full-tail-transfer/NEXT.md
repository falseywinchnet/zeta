# Next proof obligations

1. Normalize the one-sided gap by `h=r-m` and bound the first two derivatives
   of the analytically continued `S_r(m,m+h)` using theta jets through order
   eight.  Compare the bound to `X/38` to obtain an explicit `h` radius.
2. Normalize the generic collision by `alpha+beta=1`; combine the exact fourth
   coefficient reduction and the positive `N6` fifth-order invariant with an
   order-six remainder bound to obtain a uniform epsilon radius.
3. On the complementary compact gap strip, retain lower gap bounds in the
   dominant positive-coefficient `S_r` and `J_b` polynomials and absorb the
   full theta correction by exact rational coefficient or Bernstein bounds.
4. Join that strip to the already proved resolved-tail region and reflect by
   evenness.  Do not use numerical sweeps to infer a sign.
