# Next proof obligations

1. Replace each endpoint series by the minimal dependency-preserving Taylor
   form through the common central jet `q^(4)`, with integral remainders using
   only `q^(5)` and differentiated remainders using only `q^(6)`.
2. Evaluate the complete 74-term numerator in the sparse epsilon ring and
   divide `alpha*beta^2*epsilon^5` before coefficient majorization.
3. Bound the derivative of the divided remainder uniformly for
   `alpha+beta=1`; compare it with the certified `C4` margin to obtain the true
   simultaneous-collision radius.
4. Preserve the two angular faces separately when transferring the dominant
   polynomial on the radial complement.
