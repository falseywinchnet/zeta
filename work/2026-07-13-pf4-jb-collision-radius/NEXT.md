# Next proof obligations

1. Convert the radial radius to the multiplicative gap floor
   `exp(2 epsilon0)-1 >= 2 epsilon0`.
2. Insert that exact floor into the dominant-theta `J_b` polynomial transfer,
   preserving `alpha` and `beta` rather than replacing them by independent
   endpoint-error boxes.
3. Run separate coefficient modules on the `beta=0` and `alpha=0` faces.
4. Join the collision cone, both faces, and the separated radial complement;
   only then claim positive-tail `J_b`.
