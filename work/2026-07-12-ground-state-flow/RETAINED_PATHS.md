# Retained path stack

Nothing below is discarded by choosing the ground-state flow first.

## Exact RH boundaries

1. Localized Weil ground-state limit: `lambda_a>=0` for all `a`.
2. Suzuki arithmetic convolution: `h_omega*L2(1,infinity)` lands in
   `L2(0,infinity)` for every `0<omega<1/2`.
3. Weil/Li positivity and norm identities over their full test families.
4. A valid zeta-specific limit of finite-interval characteristic functions,
   after its normalization and analytic class are fixed.

## Active analytic mechanisms

1. Piecewise ground-state form flow in `a`.
2. Endpoint-overlap control at prime-power thresholds.
3. Wiener--Hopf/Hardy-space analysis of the arithmetic convolution kernel.
4. Canonical-system or characteristic-function evolution with a
   zero-controlling normalization.

## Structural diagnostics

1. Exact PF3/PF4 classification of the Riemann kernel.
2. PF5 failure and finite-order total-positivity geometry.
3. Conditional zero-sum and finite Ritz calculations as normalization checks.
4. DIP/phase compression only when its missing artifacts and control advantage
   exist.

## Current order

Ground-state flow has the certified anchor and now an analytic propagation law,
so it receives the next move. Arithmetic convolution remains the independent
escape route. Characteristic evolution waits on normalization. PF and phase
work remain available as structural probes, not substitutes for an RH bridge.
