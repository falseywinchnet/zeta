# Audit triage through C2

This ledger evaluates the sibling audit against the post-`P000069` repository.
It covers V1--V10 and C1--C2 only. C3 and every Q item are outside the round.

| Item | Decision | Current status | Advancement action |
|---|---|---|---|
| V1 | Accept | Partly open | Add the public repository and immutable `P000069` evidence commit; design a non-mutating one-command quick/full replay. A release tag or DOI remains a refine/publication action. |
| V2 | Accept | Open | Separate quick wrapper audits from the two full 192-bit directed compact covers. Publish exact commands and output hashes. |
| V3 | Accept | Open | Remove the categorical human-readable claim. Generate and print the 37 independent coefficients of the palindromic separator numerator; describe the theta paddings accurately as code-certified. |
| V4 | Accept | Open | Add supplied Khare, Dimitrov--Xu, and Csordas--Varga literature and a bounded related-work paragraph. Do not undertake the deferred comprehensive priority search. |
| V5 | Accept | Resolved after audit | `CERT11` is integrated; the origin parity blocks, rational enclosures, and confluent transfer are in the maintained paper; the removed citation and PDF are gone. Preserve this resolution in the candidate. |
| V6 | Accept | Open | State separately that the separator discriminant is a directed 192-bit calculation. Do not otherwise audit C3. |
| V7 | Accept | Open | Prove `Phi>0` termwise for `t>=0`, use evenness for `t<0`, and only then define `log Phi`. |
| V8 | Accept | Open | Record base-cover and wrapper precision separately. The PF3 base cover is Arb 192 while its wrapper cross-check uses Arb 256; the C4 base cover requires python-flint at Arb 192 although the wrapper does not. Correct certificate metadata only in a later refine round. |
| V9 | Accept | Open | Attribute positive `kappa` to the established inequalities `q>0,F2>0`, not to strict PF3. |
| V10 | Split | Partly actionable | The candidate adds keywords, MSC, PDF metadata, code/data availability, and AI provenance. Do not invent affiliation, legal identity, contact, ORCID, funding, contributions, or conflicts. Tectonic 0.16.9 lacks the LaTeX `DocumentMetadata` tagging interface, so a tagged PDF requires a newer TeX toolchain or journal production service. |
| C1 | Accept | Open | Remove `sharp` from the title, section heading, and Fourier-separator framing. The exact PF order remains exact; no unproved Fourier threshold language remains. |
| C2 | Accept | Open | Say `global increasing coordinate on its image`. Surjectivity is unused, so proving it would add proof surface without benefit. |

## Validity priorities

The validity-relevant repairs are V2, V3, V7, V8, and V9. V1 and V4 are
publication/reproducibility boundaries. V10 is partly journal-specific. C1 and
C2 are low-cost overclaim removals. V5 requires no new mathematics.

## Promotion boundary

This advancement round produces a candidate paper, replay tool, generated
separator coefficient table, and exact promotion checklist. No MIND record or
maintained manuscript is modified until a refine round audits these artifacts.
