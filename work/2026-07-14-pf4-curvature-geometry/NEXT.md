# Next steps

1. Prove or refute global radial monotonicity `D'(y)>=0` for the Riemann kernel.
   The current samples and tail limit support it but do not enclose it.
2. Derive the first two `x`-derivatives of the pair-score integral (7) entirely
   in `eta` coordinates and isolate the exact mixing remainder.
3. Search for a coercive inequality on the pair law `P_x` that implies
   nonvanishing of (9), without assuming the zeros of `Xi`.
4. Test the proposed inequality against the Gaussian PF4 separator and the
   log-convex adversarial family before applying it to `Phi`.
5. Only after a non-tautological pair inequality survives controls, compare its
   quadratic form with Suzuki's localized Weil operator and ask whether the
   `A`-coordinate basis gives sharper complement control.
6. A later refine round should audit the Dimitrov--Xu normalization, exact
   Fourier signs in (8)--(9), and every claimed closure identity before any
   MIND integration.

