# Next proof obligations

1. Generalize the right escape bound to the mixed chart where `x<0<m` and the
   bounded anchor interval is wider than `[-1,1]`, retaining `I/B^2` rather
   than bounding `D` and `B` separately.
2. Generalize the left cubic bound when `m<0<r` or `0<m<r`, so its two anchors
   can range up to the positive-tail seam without an unproved mirror identity.
3. Derive the exact reflection/orientation formula for the regularized PF4
   numerator and its two angular faces; do not identify the faces by parity.
4. Use the escape radius `R_*` to define explicit finite domains for the two
   mixed-sign charts and list the overlaps with the positive-tail certificate.
5. Build the origin collision cone from the certified global `C4` margin,
   divided by the forced `beta(alpha+beta)^2` factor.
6. Only after these analytic joins, run directed compact chart certificates;
   do not substitute a large finite scan for a missing escape lemma.
