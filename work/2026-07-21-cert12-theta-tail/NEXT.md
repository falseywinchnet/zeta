# Next advancement

Build `PF4.CERT12ThreeModeSigns` on `PF4.CERT12ThetaTail`.

1. Define the exact three-mode-relative jet used by the compact and outer
   inequalities.
2. Connect each of its seven coordinates definitionally to
   `normalizedSeriesJet_eq_first_three_relative`.
3. Port the compact continuum certificates into narrow maintained modules,
   beginning with `clearedQ`, then `clearedF2`, then `clearedC4`.
4. Port the unscaled outer perturbation theorem separately and compose it with
   the compact theorem at `certX = 5`.
5. Export literal `clearedQ`, `clearedF2`, and `clearedC4` positivity for every
   `t >= 0`, then replay the existing global parity/kernel transport.

Do not concatenate prior work files.  Each compact certificate and the outer
theorem must build as an importable module with its own axiom audit.
