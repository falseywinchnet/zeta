# Next proof obligations

1. Add an x-frame local endpoint jet for larger `|m|`; join it to the theta
   endpoint model on an explicit anchor overlap.
2. Tile adjacent `(b,a)` bands outward from the two completed asymmetric
   bands, adapting gap width to recursive denominator conditioning.
3. Build separate near-face expansions about `a=0` and `b=0`; do not drive the
   separated recursive formula into a vanishing denominator.
4. Record each collision, face, separated, tail, and escape chart in one atlas
   with exact overlap boxes.
5. Keep `global_pf4_claim=false` until the entire compact complement has zero
   unresolved boxes.
6. Audit only completed manifested domains in the subsequent refine round.
