# Results

- Derived and exactly verified the multi-center recursive divided-difference
  identity with positive normalizer `a*b^2*(a+b)^2`.
- Replaced multiplicative `(rho,theta)` denominators by affine positive gaps
  `(b,a)` on the separated chart.
- Certified positive off-origin, negative off-origin, and mixed-origin-crossing
  test cells which the one-center model could not handle.
- Proved a directed overlap with the collision-regular Hermite chart.
- Generated and replayed a 50-box manifest closing two asymmetric gap bands
  for every `m` in `[-0.25,0.25]`, with zero unresolved boxes.

Global PF4 and the rest of the compact complement remain open.
