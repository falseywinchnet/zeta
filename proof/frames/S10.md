# Frame S10 — assembly and exact order

Source: `paper/manuscript/sections/S10-completion.tex`, CERT11 and CERT17.

## Core propositions

1. `∂ξΨ<0` for every `ξ<m<r`.
2. Every translation minor of orders one through four is strictly positive at
   strictly increasing real nodes.
3. The exact determinant at spacing `211/2000` and order five is negative.
4. Therefore the exact global PF order is four.

## Expanded because

1. `N>0` **because** `δΛ>0` and the crossing integral has a strictly positive
   continuous integrand on `(p,w)`.
2. `∂ξΨ<0` **because** the sign bridge has a negative sign, `Q(p)>0`,
   `Λ²>0`, and `N>0`.
3. Strict order-four minors **because** PO-0020 transfers the strict derivative
   sign through positive quotient factors and positive-length iterated
   integrals.
4. Orders one through three **because** `Φ>0`, strict log-concavity, `Λ>0`,
   and the corresponding lower-order quotient integrals.
5. The finite PF5 evaluator denotes the target determinant **because** its
   theta sums, index orientations, and rational spacing are proved identical
   to T0/T2 (PO-0044).
6. That determinant is negative **because** an exact rational upper enclosure
   is strictly below zero (PO-0045/CERT17).
7. Exact order four **because** Steps 3–4 give PF4 and Steps 5–6 refute PF5
   under the explicit exact-order definition.

## Optional independent route

The parity factorization `H₅=CB<0` plus the two-sided confluent limit also
produces sufficiently small negative distinct-node minors. It is retained as
an independent audit route, not required for the shortest T3 proof once the
finite witness is formalized.

## Non-goal boundary

The unique confluent threshold is a separate refinement. None of T1–T3 implies
RH, Fourier real-rootedness, or an outermost finite-spacing threshold.

Lean status: `PF4.FinalAssembly.coordinatePartialXiPsi_neg_from_determinantC4`
checks a conditional theorem about the actual derivative object: exact
orientation, determinant normalization, central transport identity, and strict
sign from explicit `Q`, curvature, jet, and determinant-positivity premises.
It does not construct the Riemann kernel or prove those global inputs. The
downstream strict-minor transfer and translation quotient object layer are
checked; the three quotient signs and PF5 evaluator bridge remain.

Status: PO-0041 `FORMAL_FRAGMENT`; PO-0042 and the exact-order assembly remain
certificate-backed pending actual-kernel sign instantiation, the quotient sign
bridges, and the PF5 bridge.
