# PO-0041 — strict derivative sign

Frame: S10

Statement: for every ordered parameter triple,

\[
\partial_\xi\Psi=-\frac{Q(p)N}{\Lambda^2}<0.
\]

Formalization: `PF4.Transport.partialXiPsi_neg_of_transport` checks the final
sign transfer from the exact displayed identity, `Q(p)>0`, `Λ>0`, and `N>0`.

Remaining input: formalize the exact derivative identity itself (PO-0027) and
instantiate the checked positive numerator (PO-0040).

Counterexample condition: a failure of the exact bridge identity or an
admissible ordered triple with `∂ξΨ≥0`.

Status: `FORMAL_FRAGMENT`.
