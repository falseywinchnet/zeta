# PO-0026 — differentiated numerator object

Frame: S06

Statement: differentiating the independently defined coordinate
`Psi = Lambda + T Lambda / Lambda` produces the displayed numerator `N`.

Formalization: `PF4.CoordinateSignBridge.coordinatePsi_hasDerivAt` derives the
actual derivative from endpoint chord objects. `coordinateNumerator` is a
separate definition, and
`coordinateNumerator_eq_deltaLambda_mul_expandedTransportK` proves its exact
expanded transport form.

Non-vacuity: neither the derivative value nor numerator identity is a premise.
Every cleared denominator follows from an ordered nonzero gap or positive
normalizer.

Counterexample condition: an admissible endpoint jet for which direct
differentiation of `coordinatePsi` differs from the displayed numerator.

Status: `FORMALLY_PROVED`.
