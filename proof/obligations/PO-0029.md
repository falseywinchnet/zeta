# PO-0029 — positive curvature rate

Frame: S07

Statement: `D > 0` globally.

Formalization: the pointwise legal theorem
`PF4.PaperObjectClosure.primitiveRate_pos_at_of_determinantC4_pos` transfers
positivity of the primary determinant through the exact PO-0028 identity and
the positive factor `Q^6 kappa^2`. The actual-kernel theorem
`actualCoordinateD_pos_on_range` instantiates it at every point of the actual
coordinate range.

No fresh positive `D` symbol, off-range positivity premise, or desired
transport sign is assumed.

Counterexample condition: positive `Q`, positive curvature, and positive
primary determinant at a point but a nonpositive primitive rate `D` there.

Status: `FORMALLY_PROVED`.
